module math {
public:

   type Vec2 :: struct {
      x, y :: f32;
   };

private:
   fn dot (a, b :: Vec2) -> f32 => a.x*b.x + a.y*b.y;
   fn dist(a, b :: Vec2) -> f32 => math.Sqrt( (a.x-b.x)**2 + (a.y-b.y)**2 );
}